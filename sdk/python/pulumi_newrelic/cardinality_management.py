# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = ['CardinalityManagementArgs', 'CardinalityManagement']

@pulumi.input_type
class CardinalityManagementArgs:
    def __init__(__self__, *,
                 mode: pulumi.Input[_builtins.str],
                 cardinality_limit: pulumi.Input[Optional[_builtins.int]] = None,
                 metrics: pulumi.Input[Optional[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]]] = None):
        """
        The set of arguments for constructing a CardinalityManagement resource.

        :param pulumi.Input[_builtins.str] mode: The override mode. Accepted values: `"DEFAULT"` or `"PER_METRIC"`. Forces re-creation when changed.
               * `DEFAULT` — sets an account-wide limit for all metrics. `cardinality_limit` is required; `metric` blocks must not be set.
               * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinality_limit` must not be set at the top level.
        :param pulumi.Input[_builtins.int] cardinality_limit: The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `"DEFAULT"`; must not be set when `mode` is `"PER_METRIC"`.
        :param pulumi.Input[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]] metrics: One or more metric override blocks. Required when `mode` is `"PER_METRIC"`; must not be set when `mode` is `"DEFAULT"`. Each block supports:
        """
        pulumi.set(__self__, "mode", mode)
        if cardinality_limit is not None:
            pulumi.set(__self__, "cardinality_limit", cardinality_limit)
        if metrics is not None:
            pulumi.set(__self__, "metrics", metrics)

    @_builtins.property
    @pulumi.getter
    def mode(self) -> pulumi.Input[_builtins.str]:
        """
        The override mode. Accepted values: `"DEFAULT"` or `"PER_METRIC"`. Forces re-creation when changed.
        * `DEFAULT` — sets an account-wide limit for all metrics. `cardinality_limit` is required; `metric` blocks must not be set.
        * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinality_limit` must not be set at the top level.
        """
        return pulumi.get(self, "mode")

    @mode.setter
    def mode(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "mode", value)

    @_builtins.property
    @pulumi.getter(name="cardinalityLimit")
    def cardinality_limit(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `"DEFAULT"`; must not be set when `mode` is `"PER_METRIC"`.
        """
        return pulumi.get(self, "cardinality_limit")

    @cardinality_limit.setter
    def cardinality_limit(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "cardinality_limit", value)

    @_builtins.property
    @pulumi.getter
    def metrics(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]]]:
        """
        One or more metric override blocks. Required when `mode` is `"PER_METRIC"`; must not be set when `mode` is `"DEFAULT"`. Each block supports:
        """
        return pulumi.get(self, "metrics")

    @metrics.setter
    def metrics(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]]]):
        pulumi.set(self, "metrics", value)


@pulumi.input_type
class _CardinalityManagementState:
    def __init__(__self__, *,
                 cardinality_limit: pulumi.Input[Optional[_builtins.int]] = None,
                 metrics: pulumi.Input[Optional[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]]] = None,
                 mode: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering CardinalityManagement resources.

        :param pulumi.Input[_builtins.int] cardinality_limit: The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `"DEFAULT"`; must not be set when `mode` is `"PER_METRIC"`.
        :param pulumi.Input[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]] metrics: One or more metric override blocks. Required when `mode` is `"PER_METRIC"`; must not be set when `mode` is `"DEFAULT"`. Each block supports:
        :param pulumi.Input[_builtins.str] mode: The override mode. Accepted values: `"DEFAULT"` or `"PER_METRIC"`. Forces re-creation when changed.
               * `DEFAULT` — sets an account-wide limit for all metrics. `cardinality_limit` is required; `metric` blocks must not be set.
               * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinality_limit` must not be set at the top level.
        """
        if cardinality_limit is not None:
            pulumi.set(__self__, "cardinality_limit", cardinality_limit)
        if metrics is not None:
            pulumi.set(__self__, "metrics", metrics)
        if mode is not None:
            pulumi.set(__self__, "mode", mode)

    @_builtins.property
    @pulumi.getter(name="cardinalityLimit")
    def cardinality_limit(self) -> pulumi.Input[Optional[_builtins.int]]:
        """
        The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `"DEFAULT"`; must not be set when `mode` is `"PER_METRIC"`.
        """
        return pulumi.get(self, "cardinality_limit")

    @cardinality_limit.setter
    def cardinality_limit(self, value: pulumi.Input[Optional[_builtins.int]]):
        pulumi.set(self, "cardinality_limit", value)

    @_builtins.property
    @pulumi.getter
    def metrics(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]]]:
        """
        One or more metric override blocks. Required when `mode` is `"PER_METRIC"`; must not be set when `mode` is `"DEFAULT"`. Each block supports:
        """
        return pulumi.get(self, "metrics")

    @metrics.setter
    def metrics(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['CardinalityManagementMetricArgs']]]]):
        pulumi.set(self, "metrics", value)

    @_builtins.property
    @pulumi.getter
    def mode(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The override mode. Accepted values: `"DEFAULT"` or `"PER_METRIC"`. Forces re-creation when changed.
        * `DEFAULT` — sets an account-wide limit for all metrics. `cardinality_limit` is required; `metric` blocks must not be set.
        * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinality_limit` must not be set at the top level.
        """
        return pulumi.get(self, "mode")

    @mode.setter
    def mode(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "mode", value)


@pulumi.type_token("newrelic:index/cardinalityManagement:CardinalityManagement")
class CardinalityManagement(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 cardinality_limit: pulumi.Input[Optional[_builtins.int]] = None,
                 metrics: pulumi.Input[Optional[Sequence[pulumi.Input[Union['CardinalityManagementMetricArgs', 'CardinalityManagementMetricArgsDict']]]]] = None,
                 mode: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        """
        Use this resource to manage cardinality limit overrides for a New Relic account.

        Dimensional metrics in New Relic are subject to a per-metric cardinality limit — the maximum number of unique attribute-value combinations a single metric name may produce per day. This resource lets you raise or lower that limit, either account-wide (for all metrics at once) or for individual named metrics.

        Two modes are available, selected via the required `mode` argument.

        > **Note:** Cardinality limit overrides are managed via reset rather than removal. Running `terraform destroy` resets the affected limit(s) back to the platform default of **100,000**. The behaviour for each mode is described in the sections below.

        ***

        ## DEFAULT Mode

        Sets a single account-wide limit that applies to every dimensional metric in the account that does not have its own per-metric override.

        ### Example

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        account_default = newrelic.CardinalityManagement("account_default",
            mode="DEFAULT",
            cardinality_limit=150000)
        ```

        ### Behaviour

        - **`pulumi up`** — submits the new default value. The change takes effect in the enforcement layer straight away.
        - **`pulumi preview` / `terraform refresh` on an existing resource** — reads the current account-wide default and reconciles state. Any drift is surfaced before the next apply.
        - **`terraform destroy`** — resets the account-wide default to the platform default of **100,000** and displays a confirmation warning.

        > **Note:** Changes may take a few minutes to be visible in the New Relic UI, particularly if affected metrics have not sent data recently.

        ***

        ## PER_METRIC Mode

        Sets individual cardinality limits for one or more named metrics. Each metric is configured in its own `metric` block and can have a different limit.

        ### Example — single metric

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        per_metric = newrelic.CardinalityManagement("per_metric",
            mode="PER_METRIC",
            metrics=[{
                "name": "http.server.duration",
                "cardinality_limit": 200000,
            }])
        ```

        ### Example — multiple metrics in one resource

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        high_cardinality_metrics = newrelic.CardinalityManagement("high_cardinality_metrics",
            mode="PER_METRIC",
            metrics=[
                {
                    "name": "http.server.duration",
                    "cardinality_limit": 200000,
                },
                {
                    "name": "otelcol_nrreceiver_incoming_request_proxy",
                    "cardinality_limit": 300000,
                },
                {
                    "name": "k8s.pod.cpu.usage",
                    "cardinality_limit": 150000,
                },
            ])
        ```

        ### Example — managing many metrics in bulk

        If you have a long list of metrics to manage, you can keep a single source of truth in `locals` and use a `dynamic` block to expand them into `metric` blocks at apply time. Adding or removing a metric is then a one-line edit.

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        high_cardinality_metrics = [
            {
                "name": "http.server.duration",
                "limit": 200000,
            },
            {
                "name": "otelcol_nrreceiver_incoming_request_proxy",
                "limit": 300000,
            },
            {
                "name": "k8s.pod.cpu.usage",
                "limit": 150000,
            },
            {
                "name": "k8s.pod.memory.usage",
                "limit": 150000,
            },
        ]
        bulk = newrelic.CardinalityManagement("bulk",
            metrics=[{
                "name": entry["value"]["name"],
                "cardinality_limit": int(entry["value"]["limit"]),
            } for entry in [{"key": k, "value": v} for k, v in sorted(high_cardinality_metrics.items())]],
            mode="PER_METRIC")
        ```

        ### Behaviour

        - **`pulumi up`** — submits one override per `metric` block. A warning is displayed as a reminder that updates may take a few minutes to be reflected in the UI.
        - **`pulumi preview` / `terraform refresh` on an existing resource** — in `PER_METRIC` mode, `cardinality_limit` values in state reflect the last configuration applied via terraform — this is the expected behaviour for this mode. A note is displayed as a reminder.
        - **`terraform destroy`** — resets each managed metric's limit to the platform default of **100,000** and displays a confirmation warning.

        > **Note:** In `PER_METRIC` mode, the `cardinality_limit` values within `metric` blocks reflect the last configuration applied via terraform. If any of these limits have been adjusted outside of terraform, run `pulumi up` to re-apply the desired values.

        ***

        ## Using Both Modes Together

        You can manage both the account-wide default and individual metric overrides at the same time:

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        account_default = newrelic.CardinalityManagement("account_default",
            mode="DEFAULT",
            cardinality_limit=150000)
        per_metric = newrelic.CardinalityManagement("per_metric",
            mode="PER_METRIC",
            metrics=[
                {
                    "name": "http.server.duration",
                    "cardinality_limit": 250000,
                },
                {
                    "name": "otelcol_nrreceiver_incoming_request_proxy",
                    "cardinality_limit": 300000,
                },
            ])
        ```

        ***

        ## Import

        Cardinality management resources can be imported using the composite ID format `<account_id>:<mode>`.

        For a `DEFAULT` override:

        ```sh
        $ pulumi import newrelic:index/cardinalityManagement:CardinalityManagement account_default 12345678:DEFAULT
        ```

        For a `PER_METRIC` override:

        ```sh
        $ pulumi import newrelic:index/cardinalityManagement:CardinalityManagement per_metric 12345678:PER_METRIC
        ```

        > **Note:** When importing a `PER_METRIC` resource, `metric` blocks cannot be auto-populated on import and must be defined manually in your configuration. Run `pulumi up` after import to re-apply the desired limits.


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.int] cardinality_limit: The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `"DEFAULT"`; must not be set when `mode` is `"PER_METRIC"`.
        :param pulumi.Input[Sequence[pulumi.Input[Union['CardinalityManagementMetricArgs', 'CardinalityManagementMetricArgsDict']]]] metrics: One or more metric override blocks. Required when `mode` is `"PER_METRIC"`; must not be set when `mode` is `"DEFAULT"`. Each block supports:
        :param pulumi.Input[_builtins.str] mode: The override mode. Accepted values: `"DEFAULT"` or `"PER_METRIC"`. Forces re-creation when changed.
               * `DEFAULT` — sets an account-wide limit for all metrics. `cardinality_limit` is required; `metric` blocks must not be set.
               * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinality_limit` must not be set at the top level.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: CardinalityManagementArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Use this resource to manage cardinality limit overrides for a New Relic account.

        Dimensional metrics in New Relic are subject to a per-metric cardinality limit — the maximum number of unique attribute-value combinations a single metric name may produce per day. This resource lets you raise or lower that limit, either account-wide (for all metrics at once) or for individual named metrics.

        Two modes are available, selected via the required `mode` argument.

        > **Note:** Cardinality limit overrides are managed via reset rather than removal. Running `terraform destroy` resets the affected limit(s) back to the platform default of **100,000**. The behaviour for each mode is described in the sections below.

        ***

        ## DEFAULT Mode

        Sets a single account-wide limit that applies to every dimensional metric in the account that does not have its own per-metric override.

        ### Example

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        account_default = newrelic.CardinalityManagement("account_default",
            mode="DEFAULT",
            cardinality_limit=150000)
        ```

        ### Behaviour

        - **`pulumi up`** — submits the new default value. The change takes effect in the enforcement layer straight away.
        - **`pulumi preview` / `terraform refresh` on an existing resource** — reads the current account-wide default and reconciles state. Any drift is surfaced before the next apply.
        - **`terraform destroy`** — resets the account-wide default to the platform default of **100,000** and displays a confirmation warning.

        > **Note:** Changes may take a few minutes to be visible in the New Relic UI, particularly if affected metrics have not sent data recently.

        ***

        ## PER_METRIC Mode

        Sets individual cardinality limits for one or more named metrics. Each metric is configured in its own `metric` block and can have a different limit.

        ### Example — single metric

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        per_metric = newrelic.CardinalityManagement("per_metric",
            mode="PER_METRIC",
            metrics=[{
                "name": "http.server.duration",
                "cardinality_limit": 200000,
            }])
        ```

        ### Example — multiple metrics in one resource

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        high_cardinality_metrics = newrelic.CardinalityManagement("high_cardinality_metrics",
            mode="PER_METRIC",
            metrics=[
                {
                    "name": "http.server.duration",
                    "cardinality_limit": 200000,
                },
                {
                    "name": "otelcol_nrreceiver_incoming_request_proxy",
                    "cardinality_limit": 300000,
                },
                {
                    "name": "k8s.pod.cpu.usage",
                    "cardinality_limit": 150000,
                },
            ])
        ```

        ### Example — managing many metrics in bulk

        If you have a long list of metrics to manage, you can keep a single source of truth in `locals` and use a `dynamic` block to expand them into `metric` blocks at apply time. Adding or removing a metric is then a one-line edit.

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        high_cardinality_metrics = [
            {
                "name": "http.server.duration",
                "limit": 200000,
            },
            {
                "name": "otelcol_nrreceiver_incoming_request_proxy",
                "limit": 300000,
            },
            {
                "name": "k8s.pod.cpu.usage",
                "limit": 150000,
            },
            {
                "name": "k8s.pod.memory.usage",
                "limit": 150000,
            },
        ]
        bulk = newrelic.CardinalityManagement("bulk",
            metrics=[{
                "name": entry["value"]["name"],
                "cardinality_limit": int(entry["value"]["limit"]),
            } for entry in [{"key": k, "value": v} for k, v in sorted(high_cardinality_metrics.items())]],
            mode="PER_METRIC")
        ```

        ### Behaviour

        - **`pulumi up`** — submits one override per `metric` block. A warning is displayed as a reminder that updates may take a few minutes to be reflected in the UI.
        - **`pulumi preview` / `terraform refresh` on an existing resource** — in `PER_METRIC` mode, `cardinality_limit` values in state reflect the last configuration applied via terraform — this is the expected behaviour for this mode. A note is displayed as a reminder.
        - **`terraform destroy`** — resets each managed metric's limit to the platform default of **100,000** and displays a confirmation warning.

        > **Note:** In `PER_METRIC` mode, the `cardinality_limit` values within `metric` blocks reflect the last configuration applied via terraform. If any of these limits have been adjusted outside of terraform, run `pulumi up` to re-apply the desired values.

        ***

        ## Using Both Modes Together

        You can manage both the account-wide default and individual metric overrides at the same time:

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        account_default = newrelic.CardinalityManagement("account_default",
            mode="DEFAULT",
            cardinality_limit=150000)
        per_metric = newrelic.CardinalityManagement("per_metric",
            mode="PER_METRIC",
            metrics=[
                {
                    "name": "http.server.duration",
                    "cardinality_limit": 250000,
                },
                {
                    "name": "otelcol_nrreceiver_incoming_request_proxy",
                    "cardinality_limit": 300000,
                },
            ])
        ```

        ***

        ## Import

        Cardinality management resources can be imported using the composite ID format `<account_id>:<mode>`.

        For a `DEFAULT` override:

        ```sh
        $ pulumi import newrelic:index/cardinalityManagement:CardinalityManagement account_default 12345678:DEFAULT
        ```

        For a `PER_METRIC` override:

        ```sh
        $ pulumi import newrelic:index/cardinalityManagement:CardinalityManagement per_metric 12345678:PER_METRIC
        ```

        > **Note:** When importing a `PER_METRIC` resource, `metric` blocks cannot be auto-populated on import and must be defined manually in your configuration. Run `pulumi up` after import to re-apply the desired limits.


        :param str resource_name: The name of the resource.
        :param CardinalityManagementArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(CardinalityManagementArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 cardinality_limit: pulumi.Input[Optional[_builtins.int]] = None,
                 metrics: pulumi.Input[Optional[Sequence[pulumi.Input[Union['CardinalityManagementMetricArgs', 'CardinalityManagementMetricArgsDict']]]]] = None,
                 mode: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = CardinalityManagementArgs.__new__(CardinalityManagementArgs)

            __props__.__dict__["cardinality_limit"] = cardinality_limit
            __props__.__dict__["metrics"] = metrics
            if mode is None and not opts.urn:
                raise TypeError("Missing required property 'mode'")
            __props__.__dict__["mode"] = mode
        super(CardinalityManagement, __self__).__init__(
            'newrelic:index/cardinalityManagement:CardinalityManagement',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            cardinality_limit: pulumi.Input[Optional[_builtins.int]] = None,
            metrics: pulumi.Input[Optional[Sequence[pulumi.Input[Union['CardinalityManagementMetricArgs', 'CardinalityManagementMetricArgsDict']]]]] = None,
            mode: pulumi.Input[Optional[_builtins.str]] = None) -> 'CardinalityManagement':
        """
        Get an existing CardinalityManagement resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.int] cardinality_limit: The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `"DEFAULT"`; must not be set when `mode` is `"PER_METRIC"`.
        :param pulumi.Input[Sequence[pulumi.Input[Union['CardinalityManagementMetricArgs', 'CardinalityManagementMetricArgsDict']]]] metrics: One or more metric override blocks. Required when `mode` is `"PER_METRIC"`; must not be set when `mode` is `"DEFAULT"`. Each block supports:
        :param pulumi.Input[_builtins.str] mode: The override mode. Accepted values: `"DEFAULT"` or `"PER_METRIC"`. Forces re-creation when changed.
               * `DEFAULT` — sets an account-wide limit for all metrics. `cardinality_limit` is required; `metric` blocks must not be set.
               * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinality_limit` must not be set at the top level.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _CardinalityManagementState.__new__(_CardinalityManagementState)

        __props__.__dict__["cardinality_limit"] = cardinality_limit
        __props__.__dict__["metrics"] = metrics
        __props__.__dict__["mode"] = mode
        return CardinalityManagement(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="cardinalityLimit")
    def cardinality_limit(self) -> pulumi.Output[Optional[_builtins.int]]:
        """
        The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `"DEFAULT"`; must not be set when `mode` is `"PER_METRIC"`.
        """
        return pulumi.get(self, "cardinality_limit")

    @_builtins.property
    @pulumi.getter
    def metrics(self) -> pulumi.Output[Optional[Sequence['outputs.CardinalityManagementMetric']]]:
        """
        One or more metric override blocks. Required when `mode` is `"PER_METRIC"`; must not be set when `mode` is `"DEFAULT"`. Each block supports:
        """
        return pulumi.get(self, "metrics")

    @_builtins.property
    @pulumi.getter
    def mode(self) -> pulumi.Output[_builtins.str]:
        """
        The override mode. Accepted values: `"DEFAULT"` or `"PER_METRIC"`. Forces re-creation when changed.
        * `DEFAULT` — sets an account-wide limit for all metrics. `cardinality_limit` is required; `metric` blocks must not be set.
        * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinality_limit` must not be set at the top level.
        """
        return pulumi.get(self, "mode")

