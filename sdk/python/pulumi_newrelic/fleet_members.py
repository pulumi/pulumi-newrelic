# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = ['FleetMembersArgs', 'FleetMembers']

@pulumi.input_type
class FleetMembersArgs:
    def __init__(__self__, *,
                 fleet_id: pulumi.Input[_builtins.str],
                 rings: pulumi.Input[Sequence[pulumi.Input['FleetMembersRingArgs']]]):
        """
        The set of arguments for constructing a FleetMembers resource.

        :param pulumi.Input[_builtins.str] fleet_id: The GUID of the fleet to manage entity assignments for.
        :param pulumi.Input[Sequence[pulumi.Input['FleetMembersRingArgs']]] rings: One or more ring blocks. Each block declares which entities Terraform should maintain in that ring. At least one `ring` block must be specified. The following arguments are supported within each `ring` block:
        """
        pulumi.set(__self__, "fleet_id", fleet_id)
        pulumi.set(__self__, "rings", rings)

    @_builtins.property
    @pulumi.getter(name="fleetId")
    def fleet_id(self) -> pulumi.Input[_builtins.str]:
        """
        The GUID of the fleet to manage entity assignments for.
        """
        return pulumi.get(self, "fleet_id")

    @fleet_id.setter
    def fleet_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "fleet_id", value)

    @_builtins.property
    @pulumi.getter
    def rings(self) -> pulumi.Input[Sequence[pulumi.Input['FleetMembersRingArgs']]]:
        """
        One or more ring blocks. Each block declares which entities Terraform should maintain in that ring. At least one `ring` block must be specified. The following arguments are supported within each `ring` block:
        """
        return pulumi.get(self, "rings")

    @rings.setter
    def rings(self, value: pulumi.Input[Sequence[pulumi.Input['FleetMembersRingArgs']]]):
        pulumi.set(self, "rings", value)


@pulumi.input_type
class _FleetMembersState:
    def __init__(__self__, *,
                 fleet_id: pulumi.Input[Optional[_builtins.str]] = None,
                 rings: pulumi.Input[Optional[Sequence[pulumi.Input['FleetMembersRingArgs']]]] = None):
        """
        Input properties used for looking up and filtering FleetMembers resources.

        :param pulumi.Input[_builtins.str] fleet_id: The GUID of the fleet to manage entity assignments for.
        :param pulumi.Input[Sequence[pulumi.Input['FleetMembersRingArgs']]] rings: One or more ring blocks. Each block declares which entities Terraform should maintain in that ring. At least one `ring` block must be specified. The following arguments are supported within each `ring` block:
        """
        if fleet_id is not None:
            pulumi.set(__self__, "fleet_id", fleet_id)
        if rings is not None:
            pulumi.set(__self__, "rings", rings)

    @_builtins.property
    @pulumi.getter(name="fleetId")
    def fleet_id(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The GUID of the fleet to manage entity assignments for.
        """
        return pulumi.get(self, "fleet_id")

    @fleet_id.setter
    def fleet_id(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "fleet_id", value)

    @_builtins.property
    @pulumi.getter
    def rings(self) -> pulumi.Input[Optional[Sequence[pulumi.Input['FleetMembersRingArgs']]]]:
        """
        One or more ring blocks. Each block declares which entities Terraform should maintain in that ring. At least one `ring` block must be specified. The following arguments are supported within each `ring` block:
        """
        return pulumi.get(self, "rings")

    @rings.setter
    def rings(self, value: pulumi.Input[Optional[Sequence[pulumi.Input['FleetMembersRingArgs']]]]):
        pulumi.set(self, "rings", value)


@pulumi.type_token("newrelic:index/fleetMembers:FleetMembers")
class FleetMembers(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 fleet_id: pulumi.Input[Optional[_builtins.str]] = None,
                 rings: pulumi.Input[Optional[Sequence[pulumi.Input[Union['FleetMembersRingArgs', 'FleetMembersRingArgsDict']]]]] = None,
                 __props__=None):
        """
        Use this resource to manage entity assignments across one or more rings of a New Relic Fleet. Each resource instance is scoped to a single fleet and may declare any number of `ring` blocks, one per ring to manage.

        > **Note** This resource requires access to the New Relic Fleet Control API. Ensure the API key provided to the provider has the necessary Fleet Control permissions.

        ## How membership management works

        Entities can join a fleet ring through two independent paths:

        1. **Agent Control instrumentation** — when an entity is instrumented with Agent Control and associated with a fleet, the platform assigns it to the fleet automatically. These entities are outside of Terraform's control unless explicitly declared (see Adopting Agent Control entities below).

        2. **Explicit assignment** — this resource manages entities via the `FleetControlAddFleetMembers` and `FleetControlRemoveFleetMembers` API mutations. Only entities listed in `entity_ids` are tracked.

        This resource operates on an **opt-in management model**: it tracks only the entities explicitly declared in each `ring` block. Entities present in a ring through any other means—including Agent Control instrumentation—are not visible to Terraform, are never reported as drift, and are never removed by this resource — unless they are explicitly added to `entity_ids`, at which point they come under full Terraform lifecycle management and can be removed from a ring or moved to another ring on the next `apply`.

        ## Warning reference

        The following warnings may appear during `plan` or `apply` operations:

        ### Entities already assigned in fleet

        This warning fires during `create` or `update` when one or more entities listed in `entity_ids` are already assigned somewhere in the fleet (including in a different ring, or via Agent Control). The API rejects duplicate add requests, so the affected entities are skipped for the add mutation. However, if those entities are already in the target ring, they are confirmed by the subsequent read and adopted into Terraform state. From that point on, Terraform manages their lifecycle: removing them from `entity_ids` will remove them from the fleet ring on the next `apply`.

        If an entity is in a *different* ring than the one declared, it must first be removed from that ring before it can be reassigned. The warning message identifies the affected entity GUIDs and indicates which ring they were targeted for.

        ### Fleet members removed outside Terraform

        This warning fires during `plan` or `apply` when an entity listed in `entity_ids` is no longer present in the ring according to the API. This indicates out-of-band drift—the entity was removed from the ring by a process other than Terraform. The next `apply` will re-add the entity to restore the declared state. To accept the removal instead, remove the entity GUID from `entity_ids` and apply again.

        A second `plan` after apply will confirm the state is clean once API-side changes have propagated.

        ## Example Usage

        ### Single ring

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.Fleet("example",
            name="my-fleet",
            managed_entity_type="HOST",
            operating_system="LINUX")
        example_fleet_members = newrelic.FleetMembers("example",
            fleet_id=example.id,
            rings=[{
                "name": "default",
                "entity_ids": [
                    "MXxOR0VQfEhPU1R8MTIzNDU2Nzg",
                    "MXxOR0VQfEhPU1R8ODc2NTQzMjE",
                ],
            }])
        ```

        ### Multiple rings

        Multiple `ring` blocks can be declared within a single resource instance to manage entity assignments across rings in one operation. Moving an entity from one ring to another is handled by updating the `entity_ids` lists in both ring blocks and applying in a single step.

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.FleetMembers("example",
            fleet_id=example_newrelic_fleet["id"],
            rings=[
                {
                    "name": "default",
                    "entity_ids": ["MXxOR0VQfEhPU1R8MTIzNDU2Nzg"],
                },
                {
                    "name": "canary",
                    "entity_ids": [
                        "MXxOR0VQfEhPU1R8ODc2NTQzMjE",
                        "MXxOR0VQfEhPU1R8OTk5ODc2NTQ",
                    ],
                },
            ])
        ```

        ### Adopting Agent Control entities

        To bring an entity that joined via Agent Control under Terraform management, add its GUID to `entity_ids`. A warning will indicate that the entity is already assigned in the fleet and was skipped during the add mutation. The entity is then confirmed by the subsequent read and adopted into state. Subsequent plans will be no-ops, and destroying the resource (or removing the GUID from `entity_ids`) will remove the entity from the fleet ring.

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.FleetMembers("example",
            fleet_id=example_newrelic_fleet["id"],
            rings=[{
                "name": "default",
                "entity_ids": [
                    "MXxOR0VQfEhPU1R8MTIzNDU2Nzg",
                    "MXxOR0VQfEhPU1R8ODc2NTQzMjE",
                ],
            }])
        ```

        ## Drift Detection

        On each `plan` or `refresh`, this resource queries the current membership of each declared ring and compares it against the entity GUIDs in state. If any declared entity is no longer present in the ring, a warning is emitted and the pending plan will include a change to re-add it. If the removal was intentional, remove the GUID from `entity_ids` to silence the warning and prevent re-addition.

        Entities present in the ring but not declared in `entity_ids` are never surfaced as drift.

        ## Import

        A `FleetMembers` resource can be imported using the fleet GUID:

        ```sh
        $ pulumi import newrelic:index/fleetMembers:FleetMembers example <fleet_guid>
        ```

        On import, the resource queries all current members of the fleet and populates a single `ring` block named `"default"` with all discovered entity GUIDs. For fleets with entities distributed across multiple rings, update the `ring` blocks in the configuration to match the actual ring topology after import and run `pulumi preview` to confirm there are no unintended changes.

        > **Note on ordering** Because `entity_ids` is an ordered list, the entity GUIDs populated on import reflect the order returned by the API. If your configuration specifies a different order, `pulumi preview` will show an in-place update to reorder the list. Reorder the `entity_ids` entries in your configuration to match the imported order, or apply once to let Terraform reorder the state.


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] fleet_id: The GUID of the fleet to manage entity assignments for.
        :param pulumi.Input[Sequence[pulumi.Input[Union['FleetMembersRingArgs', 'FleetMembersRingArgsDict']]]] rings: One or more ring blocks. Each block declares which entities Terraform should maintain in that ring. At least one `ring` block must be specified. The following arguments are supported within each `ring` block:
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: FleetMembersArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Use this resource to manage entity assignments across one or more rings of a New Relic Fleet. Each resource instance is scoped to a single fleet and may declare any number of `ring` blocks, one per ring to manage.

        > **Note** This resource requires access to the New Relic Fleet Control API. Ensure the API key provided to the provider has the necessary Fleet Control permissions.

        ## How membership management works

        Entities can join a fleet ring through two independent paths:

        1. **Agent Control instrumentation** — when an entity is instrumented with Agent Control and associated with a fleet, the platform assigns it to the fleet automatically. These entities are outside of Terraform's control unless explicitly declared (see Adopting Agent Control entities below).

        2. **Explicit assignment** — this resource manages entities via the `FleetControlAddFleetMembers` and `FleetControlRemoveFleetMembers` API mutations. Only entities listed in `entity_ids` are tracked.

        This resource operates on an **opt-in management model**: it tracks only the entities explicitly declared in each `ring` block. Entities present in a ring through any other means—including Agent Control instrumentation—are not visible to Terraform, are never reported as drift, and are never removed by this resource — unless they are explicitly added to `entity_ids`, at which point they come under full Terraform lifecycle management and can be removed from a ring or moved to another ring on the next `apply`.

        ## Warning reference

        The following warnings may appear during `plan` or `apply` operations:

        ### Entities already assigned in fleet

        This warning fires during `create` or `update` when one or more entities listed in `entity_ids` are already assigned somewhere in the fleet (including in a different ring, or via Agent Control). The API rejects duplicate add requests, so the affected entities are skipped for the add mutation. However, if those entities are already in the target ring, they are confirmed by the subsequent read and adopted into Terraform state. From that point on, Terraform manages their lifecycle: removing them from `entity_ids` will remove them from the fleet ring on the next `apply`.

        If an entity is in a *different* ring than the one declared, it must first be removed from that ring before it can be reassigned. The warning message identifies the affected entity GUIDs and indicates which ring they were targeted for.

        ### Fleet members removed outside Terraform

        This warning fires during `plan` or `apply` when an entity listed in `entity_ids` is no longer present in the ring according to the API. This indicates out-of-band drift—the entity was removed from the ring by a process other than Terraform. The next `apply` will re-add the entity to restore the declared state. To accept the removal instead, remove the entity GUID from `entity_ids` and apply again.

        A second `plan` after apply will confirm the state is clean once API-side changes have propagated.

        ## Example Usage

        ### Single ring

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.Fleet("example",
            name="my-fleet",
            managed_entity_type="HOST",
            operating_system="LINUX")
        example_fleet_members = newrelic.FleetMembers("example",
            fleet_id=example.id,
            rings=[{
                "name": "default",
                "entity_ids": [
                    "MXxOR0VQfEhPU1R8MTIzNDU2Nzg",
                    "MXxOR0VQfEhPU1R8ODc2NTQzMjE",
                ],
            }])
        ```

        ### Multiple rings

        Multiple `ring` blocks can be declared within a single resource instance to manage entity assignments across rings in one operation. Moving an entity from one ring to another is handled by updating the `entity_ids` lists in both ring blocks and applying in a single step.

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.FleetMembers("example",
            fleet_id=example_newrelic_fleet["id"],
            rings=[
                {
                    "name": "default",
                    "entity_ids": ["MXxOR0VQfEhPU1R8MTIzNDU2Nzg"],
                },
                {
                    "name": "canary",
                    "entity_ids": [
                        "MXxOR0VQfEhPU1R8ODc2NTQzMjE",
                        "MXxOR0VQfEhPU1R8OTk5ODc2NTQ",
                    ],
                },
            ])
        ```

        ### Adopting Agent Control entities

        To bring an entity that joined via Agent Control under Terraform management, add its GUID to `entity_ids`. A warning will indicate that the entity is already assigned in the fleet and was skipped during the add mutation. The entity is then confirmed by the subsequent read and adopted into state. Subsequent plans will be no-ops, and destroying the resource (or removing the GUID from `entity_ids`) will remove the entity from the fleet ring.

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.FleetMembers("example",
            fleet_id=example_newrelic_fleet["id"],
            rings=[{
                "name": "default",
                "entity_ids": [
                    "MXxOR0VQfEhPU1R8MTIzNDU2Nzg",
                    "MXxOR0VQfEhPU1R8ODc2NTQzMjE",
                ],
            }])
        ```

        ## Drift Detection

        On each `plan` or `refresh`, this resource queries the current membership of each declared ring and compares it against the entity GUIDs in state. If any declared entity is no longer present in the ring, a warning is emitted and the pending plan will include a change to re-add it. If the removal was intentional, remove the GUID from `entity_ids` to silence the warning and prevent re-addition.

        Entities present in the ring but not declared in `entity_ids` are never surfaced as drift.

        ## Import

        A `FleetMembers` resource can be imported using the fleet GUID:

        ```sh
        $ pulumi import newrelic:index/fleetMembers:FleetMembers example <fleet_guid>
        ```

        On import, the resource queries all current members of the fleet and populates a single `ring` block named `"default"` with all discovered entity GUIDs. For fleets with entities distributed across multiple rings, update the `ring` blocks in the configuration to match the actual ring topology after import and run `pulumi preview` to confirm there are no unintended changes.

        > **Note on ordering** Because `entity_ids` is an ordered list, the entity GUIDs populated on import reflect the order returned by the API. If your configuration specifies a different order, `pulumi preview` will show an in-place update to reorder the list. Reorder the `entity_ids` entries in your configuration to match the imported order, or apply once to let Terraform reorder the state.


        :param str resource_name: The name of the resource.
        :param FleetMembersArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(FleetMembersArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 fleet_id: pulumi.Input[Optional[_builtins.str]] = None,
                 rings: pulumi.Input[Optional[Sequence[pulumi.Input[Union['FleetMembersRingArgs', 'FleetMembersRingArgsDict']]]]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = FleetMembersArgs.__new__(FleetMembersArgs)

            if fleet_id is None and not opts.urn:
                raise TypeError("Missing required property 'fleet_id'")
            __props__.__dict__["fleet_id"] = fleet_id
            if rings is None and not opts.urn:
                raise TypeError("Missing required property 'rings'")
            __props__.__dict__["rings"] = rings
        super(FleetMembers, __self__).__init__(
            'newrelic:index/fleetMembers:FleetMembers',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            fleet_id: pulumi.Input[Optional[_builtins.str]] = None,
            rings: pulumi.Input[Optional[Sequence[pulumi.Input[Union['FleetMembersRingArgs', 'FleetMembersRingArgsDict']]]]] = None) -> 'FleetMembers':
        """
        Get an existing FleetMembers resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] fleet_id: The GUID of the fleet to manage entity assignments for.
        :param pulumi.Input[Sequence[pulumi.Input[Union['FleetMembersRingArgs', 'FleetMembersRingArgsDict']]]] rings: One or more ring blocks. Each block declares which entities Terraform should maintain in that ring. At least one `ring` block must be specified. The following arguments are supported within each `ring` block:
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _FleetMembersState.__new__(_FleetMembersState)

        __props__.__dict__["fleet_id"] = fleet_id
        __props__.__dict__["rings"] = rings
        return FleetMembers(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="fleetId")
    def fleet_id(self) -> pulumi.Output[_builtins.str]:
        """
        The GUID of the fleet to manage entity assignments for.
        """
        return pulumi.get(self, "fleet_id")

    @_builtins.property
    @pulumi.getter
    def rings(self) -> pulumi.Output[Sequence['outputs.FleetMembersRing']]:
        """
        One or more ring blocks. Each block declares which entities Terraform should maintain in that ring. At least one `ring` block must be specified. The following arguments are supported within each `ring` block:
        """
        return pulumi.get(self, "rings")

