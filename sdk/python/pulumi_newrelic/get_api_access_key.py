# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = [
    'GetApiAccessKeyResult',
    'AwaitableGetApiAccessKeyResult',
    'get_api_access_key',
    'get_api_access_key_output',
]

@pulumi.output_type
class GetApiAccessKeyResult:
    """
    A collection of values returned by getApiAccessKey.
    """
    def __init__(__self__, account_id=None, id=None, ingest_type=None, key=None, key_id=None, key_type=None, name=None, notes=None, user_id=None):
        if account_id and not isinstance(account_id, str):
            raise TypeError("Expected argument 'account_id' to be a str")
        pulumi.set(__self__, "account_id", account_id)
        if id and not isinstance(id, str):
            raise TypeError("Expected argument 'id' to be a str")
        pulumi.set(__self__, "id", id)
        if ingest_type and not isinstance(ingest_type, str):
            raise TypeError("Expected argument 'ingest_type' to be a str")
        pulumi.set(__self__, "ingest_type", ingest_type)
        if key and not isinstance(key, str):
            raise TypeError("Expected argument 'key' to be a str")
        pulumi.set(__self__, "key", key)
        if key_id and not isinstance(key_id, str):
            raise TypeError("Expected argument 'key_id' to be a str")
        pulumi.set(__self__, "key_id", key_id)
        if key_type and not isinstance(key_type, str):
            raise TypeError("Expected argument 'key_type' to be a str")
        pulumi.set(__self__, "key_type", key_type)
        if name and not isinstance(name, str):
            raise TypeError("Expected argument 'name' to be a str")
        pulumi.set(__self__, "name", name)
        if notes and not isinstance(notes, str):
            raise TypeError("Expected argument 'notes' to be a str")
        pulumi.set(__self__, "notes", notes)
        if user_id and not isinstance(user_id, str):
            raise TypeError("Expected argument 'user_id' to be a str")
        pulumi.set(__self__, "user_id", user_id)

    @_builtins.property
    @pulumi.getter(name="accountId")
    def account_id(self) -> _builtins.str:
        """
        The New Relic account ID the key belongs to.
        """
        return pulumi.get(self, "account_id")

    @_builtins.property
    @pulumi.getter
    def id(self) -> _builtins.str:
        """
        The provider-assigned unique ID for this managed resource.
        """
        return pulumi.get(self, "id")

    @_builtins.property
    @pulumi.getter(name="ingestType")
    def ingest_type(self) -> _builtins.str:
        """
        The type of the ingest key (`LICENSE` or `BROWSER`). Populated only when `key_type` is `INGEST`.
        """
        return pulumi.get(self, "ingest_type")

    @_builtins.property
    @pulumi.getter
    def key(self) -> _builtins.str:
        """
        The value of the API key. This attribute is marked as **sensitive**.
        - <span style="color:tomato;">It is important to exercise caution when exporting the value of `key`, as it is sensitive information</span>. Avoid logging or exposing it inappropriately.
        - Not populated when the New Relic API returns a truncated key value; see the warning at the top of this page.
        """
        return pulumi.get(self, "key")

    @_builtins.property
    @pulumi.getter(name="keyId")
    def key_id(self) -> _builtins.str:
        """
        The ID of the API key.
        """
        return pulumi.get(self, "key_id")

    @_builtins.property
    @pulumi.getter(name="keyType")
    def key_type(self) -> _builtins.str:
        """
        The type of the key (`INGEST` or `USER`).
        """
        return pulumi.get(self, "key_type")

    @_builtins.property
    @pulumi.getter
    def name(self) -> _builtins.str:
        """
        The name of the API key.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter
    def notes(self) -> _builtins.str:
        """
        Any notes attached to the API key.
        """
        return pulumi.get(self, "notes")

    @_builtins.property
    @pulumi.getter(name="userId")
    def user_id(self) -> _builtins.str:
        """
        The New Relic user ID that owns the key. Populated only when `key_type` is `USER`.
        """
        return pulumi.get(self, "user_id")


class AwaitableGetApiAccessKeyResult(GetApiAccessKeyResult):
    # pylint: disable=using-constant-test
    def __await__(self):
        if False:
            yield self
        return GetApiAccessKeyResult(
            account_id=self.account_id,
            id=self.id,
            ingest_type=self.ingest_type,
            key=self.key,
            key_id=self.key_id,
            key_type=self.key_type,
            name=self.name,
            notes=self.notes,
            user_id=self.user_id)


def get_api_access_key(account_id: Optional[_builtins.str] = None,
                       ingest_type: Optional[_builtins.str] = None,
                       key_id: Optional[_builtins.str] = None,
                       key_type: Optional[_builtins.str] = None,
                       name: Optional[_builtins.str] = None,
                       user_id: Optional[_builtins.str] = None,
                       opts: Optional[pulumi.InvokeOptions] = None) -> AwaitableGetApiAccessKeyResult:
    """
    Use this data source to retrieve details of an existing New Relic API access key, which can be either an [Ingest (license) key](https://docs.newrelic.com/docs/accounts/install-new-relic/account-setup/license-key) or a [User API key](https://docs.newrelic.com/docs/apis/get-started/intro-apis/types-new-relic-api-keys#user-api-key). A common use case is to fetch the default license key that New Relic creates for every account (for instance, via the `AccountManagement` resource) and inject its value into other resources, without having to manage the key itself with the `ApiAccessKey` resource.

    A key may be looked up in one of two ways: directly by its `key_id`, or by searching with a combination of `account_id`, `key_type`, `ingest_type`, `user_id` and `name`.

    Refer to the New Relic article ['Use NerdGraph to manage license keys and User API keys'](https://docs.newrelic.com/docs/apis/nerdgraph/examples/use-nerdgraph-manage-license-keys-user-keys) for detailed information.

    > **WARNING:** If the key being fetched was created for a user other than the one whose API key is being used to run Terraform, the New Relic API returns a <span style="color:tomato;">truncated key value</span> for security reasons. In such a case, the `key` attribute is not populated in state and a warning is emitted. See the Attributes Reference section below, and the New Relic article ['Use NerdGraph to manage license keys and User API keys'](https://docs.newrelic.com/docs/apis/nerdgraph/examples/use-nerdgraph-manage-license-keys-user-keys/#query-keys) for further details.

    ## Example Usage

    ### Example: Look up the default license key of an account

    ```python
    import pulumi
    import pulumi_newrelic as newrelic

    default = newrelic.get_api_access_key(account_id="1234567",
        key_type="INGEST",
        ingest_type="LICENSE",
        name="License Key for <Account name>")
    ```

    ### Example: Look up a key directly by its ID

    ```python
    import pulumi
    import pulumi_newrelic as newrelic

    by_id = newrelic.get_api_access_key(key_id="131313133A331313130B5F13DF01313FDB13B13133EE5E133D13EAAB3A3C13D3",
        key_type="INGEST")
    ```

    ### Example: Inject a license key into another resource

    ```python
    import pulumi
    import pulumi_newrelic as newrelic

    ingest = newrelic.get_api_access_key(account_id="1234567",
        key_type="INGEST",
        ingest_type="LICENSE")
    pulumi.export("licenseKey", ingest.key)
    ```


    :param _builtins.str account_id: The New Relic account ID to search for keys in. Defaults to the `account_id` set in the `provider{}` block (or the `NEW_RELIC_ACCOUNT_ID` environment variable) if not specified.
    :param _builtins.str ingest_type: Filters the search to ingest keys of the given type. Valid options are `LICENSE` or `BROWSER` (case-sensitive). Only applies when `key_type` is `INGEST`.
    :param _builtins.str key_id: The ID of the key to look up. When specified, the key is fetched directly by its ID and all other search arguments are ignored.
    :param _builtins.str key_type: The type of the key to look up. Valid options are `INGEST` or `USER` (case-sensitive).
    :param _builtins.str name: Filters the search to keys with the given name.
           
           > **NOTE:** When `key_id` is not specified, the search must resolve to exactly one key. If zero or more than one key matches the given criteria, the data source returns an error. In such a case, provide additional arguments (such as `name`, `ingest_type` or `user_id`) to identify a unique key, or specify `key_id` directly to bypass the search.
    :param _builtins.str user_id: Filters the search to user keys owned by the given New Relic user ID. Only applies when `key_type` is `USER`.
    """
    __args__ = dict()
    __args__['accountId'] = account_id
    __args__['ingestType'] = ingest_type
    __args__['keyId'] = key_id
    __args__['keyType'] = key_type
    __args__['name'] = name
    __args__['userId'] = user_id
    opts = pulumi.InvokeOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke('newrelic:index/getApiAccessKey:getApiAccessKey', __args__, opts=opts, typ=GetApiAccessKeyResult).value

    return AwaitableGetApiAccessKeyResult(
        account_id=pulumi.get(__ret__, 'account_id'),
        id=pulumi.get(__ret__, 'id'),
        ingest_type=pulumi.get(__ret__, 'ingest_type'),
        key=pulumi.get(__ret__, 'key'),
        key_id=pulumi.get(__ret__, 'key_id'),
        key_type=pulumi.get(__ret__, 'key_type'),
        name=pulumi.get(__ret__, 'name'),
        notes=pulumi.get(__ret__, 'notes'),
        user_id=pulumi.get(__ret__, 'user_id'))
def get_api_access_key_output(account_id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                              ingest_type: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                              key_id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                              key_type: pulumi.Input[Optional[_builtins.str]] = None,
                              name: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                              user_id: pulumi.Input[Optional[Optional[_builtins.str]]] = None,
                              opts: Optional[Union[pulumi.InvokeOptions, pulumi.InvokeOutputOptions]] = None) -> pulumi.Output[GetApiAccessKeyResult]:
    """
    Use this data source to retrieve details of an existing New Relic API access key, which can be either an [Ingest (license) key](https://docs.newrelic.com/docs/accounts/install-new-relic/account-setup/license-key) or a [User API key](https://docs.newrelic.com/docs/apis/get-started/intro-apis/types-new-relic-api-keys#user-api-key). A common use case is to fetch the default license key that New Relic creates for every account (for instance, via the `AccountManagement` resource) and inject its value into other resources, without having to manage the key itself with the `ApiAccessKey` resource.

    A key may be looked up in one of two ways: directly by its `key_id`, or by searching with a combination of `account_id`, `key_type`, `ingest_type`, `user_id` and `name`.

    Refer to the New Relic article ['Use NerdGraph to manage license keys and User API keys'](https://docs.newrelic.com/docs/apis/nerdgraph/examples/use-nerdgraph-manage-license-keys-user-keys) for detailed information.

    > **WARNING:** If the key being fetched was created for a user other than the one whose API key is being used to run Terraform, the New Relic API returns a <span style="color:tomato;">truncated key value</span> for security reasons. In such a case, the `key` attribute is not populated in state and a warning is emitted. See the Attributes Reference section below, and the New Relic article ['Use NerdGraph to manage license keys and User API keys'](https://docs.newrelic.com/docs/apis/nerdgraph/examples/use-nerdgraph-manage-license-keys-user-keys/#query-keys) for further details.

    ## Example Usage

    ### Example: Look up the default license key of an account

    ```python
    import pulumi
    import pulumi_newrelic as newrelic

    default = newrelic.get_api_access_key(account_id="1234567",
        key_type="INGEST",
        ingest_type="LICENSE",
        name="License Key for <Account name>")
    ```

    ### Example: Look up a key directly by its ID

    ```python
    import pulumi
    import pulumi_newrelic as newrelic

    by_id = newrelic.get_api_access_key(key_id="131313133A331313130B5F13DF01313FDB13B13133EE5E133D13EAAB3A3C13D3",
        key_type="INGEST")
    ```

    ### Example: Inject a license key into another resource

    ```python
    import pulumi
    import pulumi_newrelic as newrelic

    ingest = newrelic.get_api_access_key(account_id="1234567",
        key_type="INGEST",
        ingest_type="LICENSE")
    pulumi.export("licenseKey", ingest.key)
    ```


    :param _builtins.str account_id: The New Relic account ID to search for keys in. Defaults to the `account_id` set in the `provider{}` block (or the `NEW_RELIC_ACCOUNT_ID` environment variable) if not specified.
    :param _builtins.str ingest_type: Filters the search to ingest keys of the given type. Valid options are `LICENSE` or `BROWSER` (case-sensitive). Only applies when `key_type` is `INGEST`.
    :param _builtins.str key_id: The ID of the key to look up. When specified, the key is fetched directly by its ID and all other search arguments are ignored.
    :param _builtins.str key_type: The type of the key to look up. Valid options are `INGEST` or `USER` (case-sensitive).
    :param _builtins.str name: Filters the search to keys with the given name.
           
           > **NOTE:** When `key_id` is not specified, the search must resolve to exactly one key. If zero or more than one key matches the given criteria, the data source returns an error. In such a case, provide additional arguments (such as `name`, `ingest_type` or `user_id`) to identify a unique key, or specify `key_id` directly to bypass the search.
    :param _builtins.str user_id: Filters the search to user keys owned by the given New Relic user ID. Only applies when `key_type` is `USER`.
    """
    __args__ = dict()
    __args__['accountId'] = account_id
    __args__['ingestType'] = ingest_type
    __args__['keyId'] = key_id
    __args__['keyType'] = key_type
    __args__['name'] = name
    __args__['userId'] = user_id
    opts = pulumi.InvokeOutputOptions.merge(_utilities.get_invoke_opts_defaults(), opts)
    __ret__ = pulumi.runtime.invoke_output('newrelic:index/getApiAccessKey:getApiAccessKey', __args__, opts=opts, typ=GetApiAccessKeyResult)
    return __ret__.apply(lambda __response__: GetApiAccessKeyResult(
        account_id=pulumi.get(__response__, 'account_id'),
        id=pulumi.get(__response__, 'id'),
        ingest_type=pulumi.get(__response__, 'ingest_type'),
        key=pulumi.get(__response__, 'key'),
        key_id=pulumi.get(__response__, 'key_id'),
        key_type=pulumi.get(__response__, 'key_type'),
        name=pulumi.get(__response__, 'name'),
        notes=pulumi.get(__response__, 'notes'),
        user_id=pulumi.get(__response__, 'user_id')))
