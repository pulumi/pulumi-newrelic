# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities

__all__ = ['MetricPruningRuleArgs', 'MetricPruningRule']

@pulumi.input_type
class MetricPruningRuleArgs:
    def __init__(__self__, *,
                 nrql: pulumi.Input[_builtins.str],
                 account_id: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None):
        """
        The set of arguments for constructing a MetricPruningRule resource.

        :param pulumi.Input[_builtins.str] nrql: The NRQL query that identifies the metric and the specific attributes to prune. The `SELECT` clause must name the attributes to remove (not `SELECT *`), and the `FROM` clause must target `Metric`. Example: `SELECT collector.name FROM Metric WHERE metricName = 'my.metric.name'`.
        :param pulumi.Input[_builtins.str] account_id: The New Relic account ID in which to create the pruning rule. Defaults to the account ID configured on the provider.
               
               > **Note:** All arguments on this resource are immutable. Any change to an existing `MetricPruningRule` will cause the resource to be destroyed and recreated with the updated configuration.
        :param pulumi.Input[_builtins.str] description: A human-readable description of what this pruning rule does.
        """
        pulumi.set(__self__, "nrql", nrql)
        if account_id is not None:
            pulumi.set(__self__, "account_id", account_id)
        if description is not None:
            pulumi.set(__self__, "description", description)

    @_builtins.property
    @pulumi.getter
    def nrql(self) -> pulumi.Input[_builtins.str]:
        """
        The NRQL query that identifies the metric and the specific attributes to prune. The `SELECT` clause must name the attributes to remove (not `SELECT *`), and the `FROM` clause must target `Metric`. Example: `SELECT collector.name FROM Metric WHERE metricName = 'my.metric.name'`.
        """
        return pulumi.get(self, "nrql")

    @nrql.setter
    def nrql(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "nrql", value)

    @_builtins.property
    @pulumi.getter(name="accountId")
    def account_id(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The New Relic account ID in which to create the pruning rule. Defaults to the account ID configured on the provider.

        > **Note:** All arguments on this resource are immutable. Any change to an existing `MetricPruningRule` will cause the resource to be destroyed and recreated with the updated configuration.
        """
        return pulumi.get(self, "account_id")

    @account_id.setter
    def account_id(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "account_id", value)

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        A human-readable description of what this pruning rule does.
        """
        return pulumi.get(self, "description")

    @description.setter
    def description(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "description", value)


@pulumi.input_type
class _MetricPruningRuleState:
    def __init__(__self__, *,
                 account_id: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 nrql: pulumi.Input[Optional[_builtins.str]] = None,
                 rule_id: pulumi.Input[Optional[_builtins.str]] = None):
        """
        Input properties used for looking up and filtering MetricPruningRule resources.

        :param pulumi.Input[_builtins.str] account_id: The New Relic account ID in which to create the pruning rule. Defaults to the account ID configured on the provider.
               
               > **Note:** All arguments on this resource are immutable. Any change to an existing `MetricPruningRule` will cause the resource to be destroyed and recreated with the updated configuration.
        :param pulumi.Input[_builtins.str] description: A human-readable description of what this pruning rule does.
        :param pulumi.Input[_builtins.str] nrql: The NRQL query that identifies the metric and the specific attributes to prune. The `SELECT` clause must name the attributes to remove (not `SELECT *`), and the `FROM` clause must target `Metric`. Example: `SELECT collector.name FROM Metric WHERE metricName = 'my.metric.name'`.
        :param pulumi.Input[_builtins.str] rule_id: The unique identifier of the pruning rule assigned by New Relic.
        """
        if account_id is not None:
            pulumi.set(__self__, "account_id", account_id)
        if description is not None:
            pulumi.set(__self__, "description", description)
        if nrql is not None:
            pulumi.set(__self__, "nrql", nrql)
        if rule_id is not None:
            pulumi.set(__self__, "rule_id", rule_id)

    @_builtins.property
    @pulumi.getter(name="accountId")
    def account_id(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The New Relic account ID in which to create the pruning rule. Defaults to the account ID configured on the provider.

        > **Note:** All arguments on this resource are immutable. Any change to an existing `MetricPruningRule` will cause the resource to be destroyed and recreated with the updated configuration.
        """
        return pulumi.get(self, "account_id")

    @account_id.setter
    def account_id(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "account_id", value)

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        A human-readable description of what this pruning rule does.
        """
        return pulumi.get(self, "description")

    @description.setter
    def description(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "description", value)

    @_builtins.property
    @pulumi.getter
    def nrql(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The NRQL query that identifies the metric and the specific attributes to prune. The `SELECT` clause must name the attributes to remove (not `SELECT *`), and the `FROM` clause must target `Metric`. Example: `SELECT collector.name FROM Metric WHERE metricName = 'my.metric.name'`.
        """
        return pulumi.get(self, "nrql")

    @nrql.setter
    def nrql(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "nrql", value)

    @_builtins.property
    @pulumi.getter(name="ruleId")
    def rule_id(self) -> pulumi.Input[Optional[_builtins.str]]:
        """
        The unique identifier of the pruning rule assigned by New Relic.
        """
        return pulumi.get(self, "rule_id")

    @rule_id.setter
    def rule_id(self, value: pulumi.Input[Optional[_builtins.str]]):
        pulumi.set(self, "rule_id", value)


@pulumi.type_token("newrelic:index/metricPruningRule:MetricPruningRule")
class MetricPruningRule(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 account_id: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 nrql: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        """
        Use this resource to create and manage metric pruning rules for a New Relic account.

        A metric pruning rule strips specific high-cardinality attributes from dimensional metric aggregates before they are stored. Unlike dropping a metric entirely, pruning keeps the metric signal intact while removing the nominated attributes — reducing cardinality without any loss of the metric itself.

        ***

        ## Example Usage

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.MetricPruningRule("example",
            nrql="SELECT collector.name FROM Metric WHERE metricName = 'http.server.duration'",
            description="Remove collector.name attribute from http.server.duration to reduce cardinality")
        ```

        ### With an explicit account ID

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.MetricPruningRule("example",
            account_id="12345678",
            nrql="SELECT collector.name FROM Metric WHERE metricName = 'http.server.duration'",
            description="Remove collector.name attribute from http.server.duration to reduce cardinality")
        ```

        ### Pruning the same attribute from many metrics in bulk

        When the same attribute needs to be stripped from a set of metrics, keep the metric list in `locals` and use `for_each` with a templated `nrql` and `description`. Only the metric name varies between rules — the rest of the configuration is shared.

        ```python
        import pulumi
        from typing import Any
        import pulumi_newrelic as newrelic
        import pulumi_std as std

        # The attribute to prune from every metric in the list below.
        pruned_attribute = "collector.name"
        # Metrics to apply the pruning rule to.
        # Add or remove entries here to manage rules in bulk.
        metrics_to_prune = std.toset(input=[
            "http.server.duration",
            "http.client.duration",
            "rpc.server.duration",
            "k8s.pod.cpu.usage",
            "k8s.pod.memory.usage",
        ]).result
        bulk: list[newrelic.MetricPruningRule] = []
        for bulk_range in [{"key": k, "value": v} for [k, v] in enumerate(metrics_to_prune)]:
            bulk.append(newrelic.MetricPruningRule(f"bulk-{bulk_range['key']}",
                nrql=f"SELECT {pruned_attribute} FROM Metric WHERE metricName = '{bulk_range['value']}'",
                description=f"Remove {pruned_attribute} from {bulk_range['value']} to reduce cardinality"))
        ```

        Each entry in `metrics_to_prune` produces an independent `MetricPruningRule` resource (e.g. `newrelic_metric_pruning_rule.bulk["http.server.duration"]`) that can be inspected, imported, or destroyed individually.

        ***

        ## Behaviour

        - **`pulumi up`** — creates the pruning rule in New Relic. The rule begins stripping the nominated attributes from matching metric aggregates immediately after creation.
        - **`pulumi preview` / `terraform refresh` on an existing resource** — reads the current state of the pruning rule from New Relic and surfaces any drift (e.g. if the rule was deleted outside of Terraform).
        - **`terraform destroy`** — permanently deletes the pruning rule. Once removed, the nominated attributes will no longer be stripped from incoming metric data. There is no reset to a default state; the rule is deleted outright.

        > **Note:** Because all arguments are immutable, any in-place change (e.g. updating the NRQL or description) will trigger a destroy-and-recreate. The old rule is deleted before the new one is created, so there will be a brief window during which no pruning is active for the affected metric.

        ***

        ## Import

        Metric pruning rules can be imported using the composite ID format `<account_id>:<rule_id>`:

        ```sh
        $ pulumi import newrelic:index/metricPruningRule:MetricPruningRule example 12345678:1234
        ```


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] account_id: The New Relic account ID in which to create the pruning rule. Defaults to the account ID configured on the provider.
               
               > **Note:** All arguments on this resource are immutable. Any change to an existing `MetricPruningRule` will cause the resource to be destroyed and recreated with the updated configuration.
        :param pulumi.Input[_builtins.str] description: A human-readable description of what this pruning rule does.
        :param pulumi.Input[_builtins.str] nrql: The NRQL query that identifies the metric and the specific attributes to prune. The `SELECT` clause must name the attributes to remove (not `SELECT *`), and the `FROM` clause must target `Metric`. Example: `SELECT collector.name FROM Metric WHERE metricName = 'my.metric.name'`.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: MetricPruningRuleArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Use this resource to create and manage metric pruning rules for a New Relic account.

        A metric pruning rule strips specific high-cardinality attributes from dimensional metric aggregates before they are stored. Unlike dropping a metric entirely, pruning keeps the metric signal intact while removing the nominated attributes — reducing cardinality without any loss of the metric itself.

        ***

        ## Example Usage

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.MetricPruningRule("example",
            nrql="SELECT collector.name FROM Metric WHERE metricName = 'http.server.duration'",
            description="Remove collector.name attribute from http.server.duration to reduce cardinality")
        ```

        ### With an explicit account ID

        ```python
        import pulumi
        import pulumi_newrelic as newrelic

        example = newrelic.MetricPruningRule("example",
            account_id="12345678",
            nrql="SELECT collector.name FROM Metric WHERE metricName = 'http.server.duration'",
            description="Remove collector.name attribute from http.server.duration to reduce cardinality")
        ```

        ### Pruning the same attribute from many metrics in bulk

        When the same attribute needs to be stripped from a set of metrics, keep the metric list in `locals` and use `for_each` with a templated `nrql` and `description`. Only the metric name varies between rules — the rest of the configuration is shared.

        ```python
        import pulumi
        from typing import Any
        import pulumi_newrelic as newrelic
        import pulumi_std as std

        # The attribute to prune from every metric in the list below.
        pruned_attribute = "collector.name"
        # Metrics to apply the pruning rule to.
        # Add or remove entries here to manage rules in bulk.
        metrics_to_prune = std.toset(input=[
            "http.server.duration",
            "http.client.duration",
            "rpc.server.duration",
            "k8s.pod.cpu.usage",
            "k8s.pod.memory.usage",
        ]).result
        bulk: list[newrelic.MetricPruningRule] = []
        for bulk_range in [{"key": k, "value": v} for [k, v] in enumerate(metrics_to_prune)]:
            bulk.append(newrelic.MetricPruningRule(f"bulk-{bulk_range['key']}",
                nrql=f"SELECT {pruned_attribute} FROM Metric WHERE metricName = '{bulk_range['value']}'",
                description=f"Remove {pruned_attribute} from {bulk_range['value']} to reduce cardinality"))
        ```

        Each entry in `metrics_to_prune` produces an independent `MetricPruningRule` resource (e.g. `newrelic_metric_pruning_rule.bulk["http.server.duration"]`) that can be inspected, imported, or destroyed individually.

        ***

        ## Behaviour

        - **`pulumi up`** — creates the pruning rule in New Relic. The rule begins stripping the nominated attributes from matching metric aggregates immediately after creation.
        - **`pulumi preview` / `terraform refresh` on an existing resource** — reads the current state of the pruning rule from New Relic and surfaces any drift (e.g. if the rule was deleted outside of Terraform).
        - **`terraform destroy`** — permanently deletes the pruning rule. Once removed, the nominated attributes will no longer be stripped from incoming metric data. There is no reset to a default state; the rule is deleted outright.

        > **Note:** Because all arguments are immutable, any in-place change (e.g. updating the NRQL or description) will trigger a destroy-and-recreate. The old rule is deleted before the new one is created, so there will be a brief window during which no pruning is active for the affected metric.

        ***

        ## Import

        Metric pruning rules can be imported using the composite ID format `<account_id>:<rule_id>`:

        ```sh
        $ pulumi import newrelic:index/metricPruningRule:MetricPruningRule example 12345678:1234
        ```


        :param str resource_name: The name of the resource.
        :param MetricPruningRuleArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(MetricPruningRuleArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 account_id: pulumi.Input[Optional[_builtins.str]] = None,
                 description: pulumi.Input[Optional[_builtins.str]] = None,
                 nrql: pulumi.Input[Optional[_builtins.str]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = MetricPruningRuleArgs.__new__(MetricPruningRuleArgs)

            __props__.__dict__["account_id"] = account_id
            __props__.__dict__["description"] = description
            if nrql is None and not opts.urn:
                raise TypeError("Missing required property 'nrql'")
            __props__.__dict__["nrql"] = nrql
            __props__.__dict__["rule_id"] = None
        super(MetricPruningRule, __self__).__init__(
            'newrelic:index/metricPruningRule:MetricPruningRule',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            account_id: pulumi.Input[Optional[_builtins.str]] = None,
            description: pulumi.Input[Optional[_builtins.str]] = None,
            nrql: pulumi.Input[Optional[_builtins.str]] = None,
            rule_id: pulumi.Input[Optional[_builtins.str]] = None) -> 'MetricPruningRule':
        """
        Get an existing MetricPruningRule resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] account_id: The New Relic account ID in which to create the pruning rule. Defaults to the account ID configured on the provider.
               
               > **Note:** All arguments on this resource are immutable. Any change to an existing `MetricPruningRule` will cause the resource to be destroyed and recreated with the updated configuration.
        :param pulumi.Input[_builtins.str] description: A human-readable description of what this pruning rule does.
        :param pulumi.Input[_builtins.str] nrql: The NRQL query that identifies the metric and the specific attributes to prune. The `SELECT` clause must name the attributes to remove (not `SELECT *`), and the `FROM` clause must target `Metric`. Example: `SELECT collector.name FROM Metric WHERE metricName = 'my.metric.name'`.
        :param pulumi.Input[_builtins.str] rule_id: The unique identifier of the pruning rule assigned by New Relic.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _MetricPruningRuleState.__new__(_MetricPruningRuleState)

        __props__.__dict__["account_id"] = account_id
        __props__.__dict__["description"] = description
        __props__.__dict__["nrql"] = nrql
        __props__.__dict__["rule_id"] = rule_id
        return MetricPruningRule(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter(name="accountId")
    def account_id(self) -> pulumi.Output[_builtins.str]:
        """
        The New Relic account ID in which to create the pruning rule. Defaults to the account ID configured on the provider.

        > **Note:** All arguments on this resource are immutable. Any change to an existing `MetricPruningRule` will cause the resource to be destroyed and recreated with the updated configuration.
        """
        return pulumi.get(self, "account_id")

    @_builtins.property
    @pulumi.getter
    def description(self) -> pulumi.Output[Optional[_builtins.str]]:
        """
        A human-readable description of what this pruning rule does.
        """
        return pulumi.get(self, "description")

    @_builtins.property
    @pulumi.getter
    def nrql(self) -> pulumi.Output[_builtins.str]:
        """
        The NRQL query that identifies the metric and the specific attributes to prune. The `SELECT` clause must name the attributes to remove (not `SELECT *`), and the `FROM` clause must target `Metric`. Example: `SELECT collector.name FROM Metric WHERE metricName = 'my.metric.name'`.
        """
        return pulumi.get(self, "nrql")

    @_builtins.property
    @pulumi.getter(name="ruleId")
    def rule_id(self) -> pulumi.Output[_builtins.str]:
        """
        The unique identifier of the pruning rule assigned by New Relic.
        """
        return pulumi.get(self, "rule_id")

