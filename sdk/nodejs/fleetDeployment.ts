// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Use this resource to create and manage New Relic fleet deployments.
 *
 * A fleet deployment defines the agent versions and optional configuration versions to roll out to a fleet. Each deployment belongs to a fleet and contains one or more `agent` blocks describing which agent type and version to deploy, and optionally which configuration version (from `newrelic.FleetConfiguration`) to apply.
 *
 * > **Note:** Deployments can only be updated while in the `CREATED` phase. Once the fleet backend begins executing the deployment (phase advances to `IN_PROGRESS`, `FAILED`, or `COMPLETED`), any attempt to change `name`, `description`, `agent`, or `tags` will be **blocked at plan time** with an error. Run `terraform destroy` to remove the deployment from state and then re-create it with the desired configuration. If `terraform destroy` itself fails because the deployment is actively executing, the resource will be removed from Terraform state with a warning — once the deployment reaches a terminal phase (`COMPLETED` or `FAILED`) you can clean it up manually in the New Relic UI.
 *
 * ## Example Usage
 *
 * ### Basic Deployment
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as newrelic from "@pulumi/newrelic";
 *
 * const infra = new newrelic.FleetDeployment("infra", {
 *     fleetId: prod.id,
 *     name: "Production Infra Deployment",
 *     description: "Deploys NRInfra v1.58.0 to the production fleet",
 *     agents: [{
 *         agentType: "NRInfra",
 *         version: "1.58.0",
 *     }],
 * });
 * ```
 *
 * ### Deployment Linked to a Configuration Version
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as newrelic from "@pulumi/newrelic";
 *
 * const infraCfg = new newrelic.FleetConfiguration("infra_cfg", {
 *     name: "Production Infra Config",
 *     agentType: "NRInfra",
 *     managedEntityType: "HOST",
 *     versions: [{
 *         configurationContent: `log:
 *   level: info
 * `,
 *     }],
 * });
 * const infra = new newrelic.FleetDeployment("infra", {
 *     fleetId: prod.id,
 *     name: "Production Infra Deployment",
 *     description: "Deploys NRInfra v1.58.0 with the production config",
 *     agents: [{
 *         agentType: "NRInfra",
 *         version: "1.58.0",
 *         configurationVersionId: infraCfg.latestVersionEntityId,
 *     }],
 * });
 * ```
 *
 * ### Multiple Agents
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as newrelic from "@pulumi/newrelic";
 *
 * const fullStack = new newrelic.FleetDeployment("full_stack", {
 *     fleetId: prod.id,
 *     name: "Full Stack Deployment",
 *     agents: [
 *         {
 *             agentType: "NRInfra",
 *             version: "1.58.0",
 *             configurationVersionId: infraCfg.latestVersionEntityId,
 *         },
 *         {
 *             agentType: "FluentBit",
 *             version: "3.2.0",
 *         },
 *     ],
 * });
 * ```
 *
 * ### With Tags
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as newrelic from "@pulumi/newrelic";
 *
 * const infra = new newrelic.FleetDeployment("infra", {
 *     fleetId: prod.id,
 *     name: "Production Deployment",
 *     agents: [{
 *         agentType: "NRInfra",
 *         version: "1.58.0",
 *     }],
 *     tags: [
 *         "environment:production",
 *         "team:platform",
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * Fleet deployments can be imported using the deployment entity GUID:
 *
 * ```sh
 * $ pulumi import newrelic:index/fleetDeployment:FleetDeployment infra <deployment_guid>
 * ```
 */
export class FleetDeployment extends pulumi.CustomResource {
    /**
     * Get an existing FleetDeployment resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: FleetDeploymentState, opts?: pulumi.CustomResourceOptions): FleetDeployment {
        return new FleetDeployment(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'newrelic:index/fleetDeployment:FleetDeployment';

    /**
     * Returns true if the given object is an instance of FleetDeployment.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is FleetDeployment {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FleetDeployment.__pulumiType;
    }

    /**
     * One or more agent blocks. At least one is required when creating a deployment. On update, the list may be set to empty (`agent = []`) to uninstall all agent assignments from the deployment. Each `agentType` may appear at most once per deployment. See Nested `agent` blocks below.
     */
    declare public readonly agents: pulumi.Output<outputs.FleetDeploymentAgent[] | undefined>;
    /**
     * The entity GUID of the deployment.
     */
    declare public /*out*/ readonly deploymentId: pulumi.Output<string>;
    /**
     * A description of the deployment.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * The entity GUID of the fleet this deployment belongs to. **Cannot be changed after creation.**
     */
    declare public readonly fleetId: pulumi.Output<string>;
    /**
     * The name of the deployment.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The organization ID. Auto-fetched from the account when not provided. **Cannot be changed after creation.**
     */
    declare public readonly organizationId: pulumi.Output<string>;
    /**
     * The current phase of the deployment. Possible values: `CREATED`, `IN_PROGRESS`, `FAILED`, `COMPLETED`.
     */
    declare public /*out*/ readonly phase: pulumi.Output<string>;
    /**
     * A list of tags in `key:value1,value2` format.
     */
    declare public readonly tags: pulumi.Output<string[] | undefined>;

    /**
     * Create a FleetDeployment resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: FleetDeploymentArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: FleetDeploymentArgs | FleetDeploymentState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as FleetDeploymentState | undefined;
            resourceInputs["agents"] = state?.agents;
            resourceInputs["deploymentId"] = state?.deploymentId;
            resourceInputs["description"] = state?.description;
            resourceInputs["fleetId"] = state?.fleetId;
            resourceInputs["name"] = state?.name;
            resourceInputs["organizationId"] = state?.organizationId;
            resourceInputs["phase"] = state?.phase;
            resourceInputs["tags"] = state?.tags;
        } else {
            const args = argsOrState as FleetDeploymentArgs | undefined;
            if (args?.fleetId === undefined && !opts.urn) {
                throw new Error("Missing required property 'fleetId'");
            }
            resourceInputs["agents"] = args?.agents;
            resourceInputs["description"] = args?.description;
            resourceInputs["fleetId"] = args?.fleetId;
            resourceInputs["name"] = args?.name;
            resourceInputs["organizationId"] = args?.organizationId;
            resourceInputs["tags"] = args?.tags;
            resourceInputs["deploymentId"] = undefined /*out*/;
            resourceInputs["phase"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FleetDeployment.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering FleetDeployment resources.
 */
export interface FleetDeploymentState {
    /**
     * One or more agent blocks. At least one is required when creating a deployment. On update, the list may be set to empty (`agent = []`) to uninstall all agent assignments from the deployment. Each `agentType` may appear at most once per deployment. See Nested `agent` blocks below.
     */
    agents?: pulumi.Input<pulumi.Input<inputs.FleetDeploymentAgent>[]>;
    /**
     * The entity GUID of the deployment.
     */
    deploymentId?: pulumi.Input<string>;
    /**
     * A description of the deployment.
     */
    description?: pulumi.Input<string>;
    /**
     * The entity GUID of the fleet this deployment belongs to. **Cannot be changed after creation.**
     */
    fleetId?: pulumi.Input<string>;
    /**
     * The name of the deployment.
     */
    name?: pulumi.Input<string>;
    /**
     * The organization ID. Auto-fetched from the account when not provided. **Cannot be changed after creation.**
     */
    organizationId?: pulumi.Input<string>;
    /**
     * The current phase of the deployment. Possible values: `CREATED`, `IN_PROGRESS`, `FAILED`, `COMPLETED`.
     */
    phase?: pulumi.Input<string>;
    /**
     * A list of tags in `key:value1,value2` format.
     */
    tags?: pulumi.Input<pulumi.Input<string>[]>;
}

/**
 * The set of arguments for constructing a FleetDeployment resource.
 */
export interface FleetDeploymentArgs {
    /**
     * One or more agent blocks. At least one is required when creating a deployment. On update, the list may be set to empty (`agent = []`) to uninstall all agent assignments from the deployment. Each `agentType` may appear at most once per deployment. See Nested `agent` blocks below.
     */
    agents?: pulumi.Input<pulumi.Input<inputs.FleetDeploymentAgent>[]>;
    /**
     * A description of the deployment.
     */
    description?: pulumi.Input<string>;
    /**
     * The entity GUID of the fleet this deployment belongs to. **Cannot be changed after creation.**
     */
    fleetId: pulumi.Input<string>;
    /**
     * The name of the deployment.
     */
    name?: pulumi.Input<string>;
    /**
     * The organization ID. Auto-fetched from the account when not provided. **Cannot be changed after creation.**
     */
    organizationId?: pulumi.Input<string>;
    /**
     * A list of tags in `key:value1,value2` format.
     */
    tags?: pulumi.Input<pulumi.Input<string>[]>;
}
