// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.newrelic.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.exceptions.MissingRequiredPropertyException;
import java.lang.Double;
import java.lang.Integer;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class NrqlAlertConditionOutlierConfigurationDbscanArgs extends com.pulumi.resources.ResourceArgs {

    public static final NrqlAlertConditionOutlierConfigurationDbscanArgs Empty = new NrqlAlertConditionOutlierConfigurationDbscanArgs();

    /**
     * BETA PREVIEW: the `epsilon` field is in limited release and only enabled for preview on a per-account basis. - Radius (distance threshold) for DBSCAN in the units of the query result. Smaller values tighten clusters; larger values broaden them. Must be &gt; 0.
     * 
     */
    @Import(name="epsilon", required=true)
    private Output<Double> epsilon;

    /**
     * @return BETA PREVIEW: the `epsilon` field is in limited release and only enabled for preview on a per-account basis. - Radius (distance threshold) for DBSCAN in the units of the query result. Smaller values tighten clusters; larger values broaden them. Must be &gt; 0.
     * 
     */
    public Output<Double> epsilon() {
        return this.epsilon;
    }

    /**
     * BETA PREVIEW: the `evaluationGroupFacet` field is in limited release and only enabled for preview on a per-account basis. - Optional NRQL facet attribute used to segment data into groups (e.g. `host`, `region`) before running outlier detection. Omit to evaluate all results together.
     * 
     */
    @Import(name="evaluationGroupFacet")
    private @Nullable Output<String> evaluationGroupFacet;

    /**
     * @return BETA PREVIEW: the `evaluationGroupFacet` field is in limited release and only enabled for preview on a per-account basis. - Optional NRQL facet attribute used to segment data into groups (e.g. `host`, `region`) before running outlier detection. Omit to evaluate all results together.
     * 
     */
    public Optional<Output<String>> evaluationGroupFacet() {
        return Optional.ofNullable(this.evaluationGroupFacet);
    }

    /**
     * BETA PREVIEW: the `minimumPoints` field is in limited release and only enabled for preview on a per-account basis. - Minimum number of neighboring points needed to form a cluster. Must be &gt;= 1.
     * 
     */
    @Import(name="minimumPoints", required=true)
    private Output<Integer> minimumPoints;

    /**
     * @return BETA PREVIEW: the `minimumPoints` field is in limited release and only enabled for preview on a per-account basis. - Minimum number of neighboring points needed to form a cluster. Must be &gt;= 1.
     * 
     */
    public Output<Integer> minimumPoints() {
        return this.minimumPoints;
    }

    private NrqlAlertConditionOutlierConfigurationDbscanArgs() {}

    private NrqlAlertConditionOutlierConfigurationDbscanArgs(NrqlAlertConditionOutlierConfigurationDbscanArgs $) {
        this.epsilon = $.epsilon;
        this.evaluationGroupFacet = $.evaluationGroupFacet;
        this.minimumPoints = $.minimumPoints;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(NrqlAlertConditionOutlierConfigurationDbscanArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private NrqlAlertConditionOutlierConfigurationDbscanArgs $;

        public Builder() {
            $ = new NrqlAlertConditionOutlierConfigurationDbscanArgs();
        }

        public Builder(NrqlAlertConditionOutlierConfigurationDbscanArgs defaults) {
            $ = new NrqlAlertConditionOutlierConfigurationDbscanArgs(Objects.requireNonNull(defaults));
        }

        /**
         * @param epsilon BETA PREVIEW: the `epsilon` field is in limited release and only enabled for preview on a per-account basis. - Radius (distance threshold) for DBSCAN in the units of the query result. Smaller values tighten clusters; larger values broaden them. Must be &gt; 0.
         * 
         * @return builder
         * 
         */
        public Builder epsilon(Output<Double> epsilon) {
            $.epsilon = epsilon;
            return this;
        }

        /**
         * @param epsilon BETA PREVIEW: the `epsilon` field is in limited release and only enabled for preview on a per-account basis. - Radius (distance threshold) for DBSCAN in the units of the query result. Smaller values tighten clusters; larger values broaden them. Must be &gt; 0.
         * 
         * @return builder
         * 
         */
        public Builder epsilon(Double epsilon) {
            return epsilon(Output.of(epsilon));
        }

        /**
         * @param evaluationGroupFacet BETA PREVIEW: the `evaluationGroupFacet` field is in limited release and only enabled for preview on a per-account basis. - Optional NRQL facet attribute used to segment data into groups (e.g. `host`, `region`) before running outlier detection. Omit to evaluate all results together.
         * 
         * @return builder
         * 
         */
        public Builder evaluationGroupFacet(@Nullable Output<String> evaluationGroupFacet) {
            $.evaluationGroupFacet = evaluationGroupFacet;
            return this;
        }

        /**
         * @param evaluationGroupFacet BETA PREVIEW: the `evaluationGroupFacet` field is in limited release and only enabled for preview on a per-account basis. - Optional NRQL facet attribute used to segment data into groups (e.g. `host`, `region`) before running outlier detection. Omit to evaluate all results together.
         * 
         * @return builder
         * 
         */
        public Builder evaluationGroupFacet(String evaluationGroupFacet) {
            return evaluationGroupFacet(Output.of(evaluationGroupFacet));
        }

        /**
         * @param minimumPoints BETA PREVIEW: the `minimumPoints` field is in limited release and only enabled for preview on a per-account basis. - Minimum number of neighboring points needed to form a cluster. Must be &gt;= 1.
         * 
         * @return builder
         * 
         */
        public Builder minimumPoints(Output<Integer> minimumPoints) {
            $.minimumPoints = minimumPoints;
            return this;
        }

        /**
         * @param minimumPoints BETA PREVIEW: the `minimumPoints` field is in limited release and only enabled for preview on a per-account basis. - Minimum number of neighboring points needed to form a cluster. Must be &gt;= 1.
         * 
         * @return builder
         * 
         */
        public Builder minimumPoints(Integer minimumPoints) {
            return minimumPoints(Output.of(minimumPoints));
        }

        public NrqlAlertConditionOutlierConfigurationDbscanArgs build() {
            if ($.epsilon == null) {
                throw new MissingRequiredPropertyException("NrqlAlertConditionOutlierConfigurationDbscanArgs", "epsilon");
            }
            if ($.minimumPoints == null) {
                throw new MissingRequiredPropertyException("NrqlAlertConditionOutlierConfigurationDbscanArgs", "minimumPoints");
            }
            return $;
        }
    }

}
