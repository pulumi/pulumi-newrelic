// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.newrelic.inputs;

import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.newrelic.inputs.CardinalityManagementMetricArgs;
import java.lang.Integer;
import java.lang.String;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


public final class CardinalityManagementState extends com.pulumi.resources.ResourceArgs {

    public static final CardinalityManagementState Empty = new CardinalityManagementState();

    /**
     * The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `&#34;DEFAULT&#34;`; must not be set when `mode` is `&#34;PER_METRIC&#34;`.
     * 
     */
    @Import(name="cardinalityLimit")
    private @Nullable Output<Integer> cardinalityLimit;

    /**
     * @return The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `&#34;DEFAULT&#34;`; must not be set when `mode` is `&#34;PER_METRIC&#34;`.
     * 
     */
    public Optional<Output<Integer>> cardinalityLimit() {
        return Optional.ofNullable(this.cardinalityLimit);
    }

    /**
     * One or more metric override blocks. Required when `mode` is `&#34;PER_METRIC&#34;`; must not be set when `mode` is `&#34;DEFAULT&#34;`. Each block supports:
     * 
     */
    @Import(name="metrics")
    private @Nullable Output<List<CardinalityManagementMetricArgs>> metrics;

    /**
     * @return One or more metric override blocks. Required when `mode` is `&#34;PER_METRIC&#34;`; must not be set when `mode` is `&#34;DEFAULT&#34;`. Each block supports:
     * 
     */
    public Optional<Output<List<CardinalityManagementMetricArgs>>> metrics() {
        return Optional.ofNullable(this.metrics);
    }

    /**
     * The override mode. Accepted values: `&#34;DEFAULT&#34;` or `&#34;PER_METRIC&#34;`. Forces re-creation when changed.
     * * `DEFAULT` — sets an account-wide limit for all metrics. `cardinalityLimit` is required; `metric` blocks must not be set.
     * * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinalityLimit` must not be set at the top level.
     * 
     */
    @Import(name="mode")
    private @Nullable Output<String> mode;

    /**
     * @return The override mode. Accepted values: `&#34;DEFAULT&#34;` or `&#34;PER_METRIC&#34;`. Forces re-creation when changed.
     * * `DEFAULT` — sets an account-wide limit for all metrics. `cardinalityLimit` is required; `metric` blocks must not be set.
     * * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinalityLimit` must not be set at the top level.
     * 
     */
    public Optional<Output<String>> mode() {
        return Optional.ofNullable(this.mode);
    }

    private CardinalityManagementState() {}

    private CardinalityManagementState(CardinalityManagementState $) {
        this.cardinalityLimit = $.cardinalityLimit;
        this.metrics = $.metrics;
        this.mode = $.mode;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(CardinalityManagementState defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private CardinalityManagementState $;

        public Builder() {
            $ = new CardinalityManagementState();
        }

        public Builder(CardinalityManagementState defaults) {
            $ = new CardinalityManagementState(Objects.requireNonNull(defaults));
        }

        /**
         * @param cardinalityLimit The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `&#34;DEFAULT&#34;`; must not be set when `mode` is `&#34;PER_METRIC&#34;`.
         * 
         * @return builder
         * 
         */
        public Builder cardinalityLimit(@Nullable Output<Integer> cardinalityLimit) {
            $.cardinalityLimit = cardinalityLimit;
            return this;
        }

        /**
         * @param cardinalityLimit The account-wide cardinality limit — the maximum number of unique dimension-value combinations allowed per metric per day. Required when `mode` is `&#34;DEFAULT&#34;`; must not be set when `mode` is `&#34;PER_METRIC&#34;`.
         * 
         * @return builder
         * 
         */
        public Builder cardinalityLimit(Integer cardinalityLimit) {
            return cardinalityLimit(Output.of(cardinalityLimit));
        }

        /**
         * @param metrics One or more metric override blocks. Required when `mode` is `&#34;PER_METRIC&#34;`; must not be set when `mode` is `&#34;DEFAULT&#34;`. Each block supports:
         * 
         * @return builder
         * 
         */
        public Builder metrics(@Nullable Output<List<CardinalityManagementMetricArgs>> metrics) {
            $.metrics = metrics;
            return this;
        }

        /**
         * @param metrics One or more metric override blocks. Required when `mode` is `&#34;PER_METRIC&#34;`; must not be set when `mode` is `&#34;DEFAULT&#34;`. Each block supports:
         * 
         * @return builder
         * 
         */
        public Builder metrics(List<CardinalityManagementMetricArgs> metrics) {
            return metrics(Output.of(metrics));
        }

        /**
         * @param metrics One or more metric override blocks. Required when `mode` is `&#34;PER_METRIC&#34;`; must not be set when `mode` is `&#34;DEFAULT&#34;`. Each block supports:
         * 
         * @return builder
         * 
         */
        public Builder metrics(CardinalityManagementMetricArgs... metrics) {
            return metrics(List.of(metrics));
        }

        /**
         * @param mode The override mode. Accepted values: `&#34;DEFAULT&#34;` or `&#34;PER_METRIC&#34;`. Forces re-creation when changed.
         * * `DEFAULT` — sets an account-wide limit for all metrics. `cardinalityLimit` is required; `metric` blocks must not be set.
         * * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinalityLimit` must not be set at the top level.
         * 
         * @return builder
         * 
         */
        public Builder mode(@Nullable Output<String> mode) {
            $.mode = mode;
            return this;
        }

        /**
         * @param mode The override mode. Accepted values: `&#34;DEFAULT&#34;` or `&#34;PER_METRIC&#34;`. Forces re-creation when changed.
         * * `DEFAULT` — sets an account-wide limit for all metrics. `cardinalityLimit` is required; `metric` blocks must not be set.
         * * `PER_METRIC` — sets individual limits for named metrics. At least one `metric` block is required; `cardinalityLimit` must not be set at the top level.
         * 
         * @return builder
         * 
         */
        public Builder mode(String mode) {
            return mode(Output.of(mode));
        }

        public CardinalityManagementState build() {
            return $;
        }
    }

}
